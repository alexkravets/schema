age:
  type: number

height:
  type: number

isNotificationEnabled:
  type: boolean

shouldSendEmails:
  type: boolean

shouldSendSMS:
  type: boolean

hasPaymentMethod:
  type: boolean

hasPositiveBalance:
  type: boolean
