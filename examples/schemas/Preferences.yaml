age:
  type: number
  min: 0
  max: 199

height:
  type: number

isNotificationEnabled:
  type: boolean

shouldSendEmails:
  type: boolean

shouldSendSMS:
  type: boolean

hasPaymentMethod:
  type: boolean

hasPositiveBalance:
  type: boolean
